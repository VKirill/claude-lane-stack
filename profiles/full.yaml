# Claude + Grok write + Codex review
pm: claude
profile: full
lanes:
  fast_write: grok         # run-supervisor watches the durable Grok controller
  main_write: grok         # run-supervisor watches the durable Grok controller
  review: codex            # codex-reviewer
  emergency_write: codex   # codex-implementer
