# Claude + AGY write (Grok selectable) + Codex review
pm: claude
profile: full
lanes:
  fast_write: agy          # run-supervisor passes --provider agy
  main_write: agy          # use --provider grok to switch
  review: codex            # codex-reviewer
  emergency_write: codex   # codex-implementer
