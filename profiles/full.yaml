# Claude + Grok write + Codex review
pm: claude
profile: full
lanes:
  fast_write: grok         # lane-supervisor controls the Grok writer
  main_write: grok         # lane-supervisor controls the Grok writer
  review: codex            # codex-reviewer
  emergency_write: codex   # codex-implementer
